#include <stdio.h>
int main()
{
int x1,x2,tmp,i;
printf("Enter x1,x2: ");
scanf("%d %d",&x1,&x2);
int a1=0, a2=1,sum=0;
for(i=x1;i<x2;i++)
{
  tmp = a1;
  if(x2>=a1 && a1>=x1)
  {
  printf("%d ",a1);
  }
  a1 = a2;
  a2 = a2 + tmp;
  sum+=a1;
}
return 0;
}
